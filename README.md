# Adding 'locus' to terminal in MacOS

1. `echo "alias locus='/Users/.../python3 /Users/.../client.py'" >> ~/.zshrc`
2. `source ~/.zshrc`
3. `locus` <- from anywhere in terminal

NOTES: 
- replace the paths with your own paths
- make sure backend is running in another terminal